public class Node { // yapı gereği LinkedList sınıfında kullanılmak için data ve next tutan node sınıfı 
    Node next;
    int data;
    public Node(int data){ // yapıcı fonksiyon
        this.data=data;
    }

}
